namespace FlightBooking.Service.Data
{
    public static class SeedData
    {
		public static void Initialize(IServiceProvider serviceProvider)
		{
			//using (var context = new FlightBookingDbContext(serviceProvider.Get)
		}
	}
}
